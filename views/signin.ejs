<!DOCTYPE html>
<html lang="en" class="dark">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Login | Blog App</title>
    <link rel="icon" href="/nft.png" />

  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-900 min-h-screen flex flex-col items-center justify-start py-10">

  <% if (locals.error) { %>
    <div class="w-full max-w-md px-4">
      <div class="p-4 mb-6 text-sm text-red-800 rounded-lg bg-red-50 dark:bg-gray-800 dark:text-red-400 shadow-md" role="alert">
        <span class="font-medium">Error:</span> <%= locals.error %>
      </div>
    </div>
  <% } %>

  <div class="flex-grow flex items-center justify-center w-full">
    <%- include('./partials/screen-components/signin-comp') %>
  </div>

</body>
</html>
